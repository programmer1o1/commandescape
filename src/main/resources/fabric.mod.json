{
  "schemaVersion": 1,
  "id": "commandescape",
  "version": "${version}",
  "name": "Command Escape",
  "description": "Use backslash to escape commands and send them as regular chat messages",
  "authors": ["Sierra"],
  "contact": {
    "homepage": "https://github.com/sierra",
    "sources": "https://github.com/sierra/commandescape"
  },
  "license": "MIT",
  "icon": "assets/commandescape/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "hi.sierra.commandescape.CommandEscapeMod"
    ]
  },
  "mixins": [
    "commandescape.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.14.0",
    "minecraft": "${mc_dep}"
  }
}